
class Stride():
    def __init__(self, stride_length, x_list, y_list, z_list) -> None:
        self.stride_length = stride_length
        self.x_list = x_list
        self.y_list = y_list
        self.z_list = z_list